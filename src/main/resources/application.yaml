spring:
  application:
    name: SpringbootEndtoEndApp
  datasource:
    #driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USER:root}
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/javatechi?useSSL=false&allowPublicKeyRetrieval=true
    password: ${MYSQL_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com   # or smtp.office365.com
    port: 587
    username: srinuvasrao966@gmail.com
    password: tccl vnxy jwsv ayhf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            protocols: TLSv1.2
            trust: smtp.gmail.com
logging:
  file:
    name: C:/Users/HI/OneDrive/Desktop/logs/springbootbackendelk.log

management:
  tracing:
    sampling:
      probability: 1.0